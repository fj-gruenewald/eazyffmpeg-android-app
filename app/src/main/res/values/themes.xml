<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EazyFFMPEG" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/appColorLight</item>
        <item name="colorPrimaryVariant">@color/appColorLight</item>
        <item name="colorOnPrimary">@color/appColorWhite</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/palettePastel_lightblue</item>
        <item name="colorSecondaryVariant">@color/palettePastel_lightblue</item>
        <item name="colorOnSecondary">@color/appColorBlack</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->

    </style>

    <!-- Style for the Theme Switch ImageView -->
    <style name="themeSwitchStyle">
        <item name="android:background">@drawable/ic_moon</item>
    </style>

</resources>